apiVersion: v1
kind: Service
metadata:
  name: homework-service
  namespace: homework
spec:
  selector:
    app: homework
  type: ClusterIP
  ports:
    - port: 8080
      protocol: TCP
      targetPort: 8000
